define(['./component', './webcomponent'], function(component, webcomponent) {

    var components = {
        component: component,
        webcomponent: webcomponent
    };


    return components;
});